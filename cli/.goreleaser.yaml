version: 2
project_name: davinci-resolve-pp-cli
changelog:
  disable: true
builds:
  - id: davinci-resolve-pp-cli
    main: ./cmd/davinci-resolve-pp-cli
    binary: davinci-resolve-pp-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/dmmdea/davinci-resolve-cli/cli/internal/cli.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: davinci-resolve-pp-mcp
    main: ./cmd/davinci-resolve-pp-mcp
    binary: davinci-resolve-pp-mcp
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{ .Version }}
    targets:
      - darwin_amd64
      - darwin_arm64
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]
checksum:
  name_template: checksums.txt
