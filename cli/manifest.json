{
  "manifest_version": "0.3",
  "name": "davinci-resolve-pp-mcp",
  "display_name": "DaVinci Resolve",
  "version": "0.1.0",
  "description": "Every documented DaVinci Resolve scripting operation, plus the cross-project history and doctor diagnostics that Resolve itself — and every other Resolve CLI or MCP server — throws away.",
  "author": {
    "name": "CLI Printing Press"
  },
  "license": "Apache-2.0",
  "server": {
    "type": "binary",
    "entry_point": "bin/davinci-resolve-pp-mcp",
    "mcp_config": {
      "command": "${__dirname}/bin/davinci-resolve-pp-mcp",
      "args": []
    }
  },
  "compatibility": {
    "claude_desktop": ">=1.0.0",
    "platforms": [
      "darwin",
      "linux",
      "win32"
    ]
  }
}
